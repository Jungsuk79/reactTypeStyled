import styled from 'styled-components';

const S = {
    NodataArea: styled.div`
        display:flex;
        align-items: center;
        justify-content: center;
        padding:50px 0;
    `
}
export default S;